[pytest]
# 让 pytest-asyncio 自动识别 @pytest.mark.asyncio 标记的协程测试。
# strict 模式可在测试中显式 mark，避免不必要的 fixture 副作用。
asyncio_mode = strict

# 测试发现根：项目根 + tests/ 子目录。
testpaths = tests

# 关闭对未使用 fixture 的告警噪音；其余 warning 仍照常输出。
filterwarnings =
    ignore::DeprecationWarning:asyncio
